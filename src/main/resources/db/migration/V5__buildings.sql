CREATE TABLE buildings (id INT AUTO_INCREMENT PRIMARY KEY,
                        type VARCHAR(45),
                        level INT (4),
                        hp INT (4),
                        started_at BIGINT (10),
                        finished_at BIGINT (10) );

